#ifndef KRM_KR_MIXER_H
#define KRM_KR_MIXER_H

#include "kr_client.h"
#include <locale.h>
#include <ncurses.h>
#include <menu.h>
#include <panel.h>
#include <form.h>
#include "mixer.h"
#include "util.h"

#define max(a,b) ((a) > (b) ? a : b)
#define min(a,b) ((a) < (b) ? a : b)

#endif

